# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- ML / embeddings ---
# CLIP model + processor come from Hugging Face transformers.
#
# IMPORTANT: install the CPU-only torch build FIRST, or pip will pull
# PyPI's default GPU build and download ~2GB of unneeded NVIDIA CUDA
# libraries. This POC only needs CPU inference:
#
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
torch==2.4.1
transformers==4.44.2

# --- Image handling ---
pillow==10.4.0

# --- Similarity search ---
scikit-learn==1.5.1
numpy==1.26.4

# --- Data validation ---
pydantic==2.9.2
